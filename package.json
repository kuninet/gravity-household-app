{
  "name": "household-app-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for household accounting app",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}